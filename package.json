{
  "name": "stop-git-std",
  "module": "src/cli.ts",
  "type": "module",
  "private": true,
  "bin": {
    "stop-git-std": "src/cli.ts"
  },
  "scripts": {
    "type-check": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "build": "bun build src/cli.ts --outdir dist --target bun",
    "start": "bun run src/cli.ts"
  },
  "dependencies": {
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
