{% set target = form.target %}
{% set head_sha = form.phase_1_raw_capture.pre_flight.head_sha or '' %}
{% set releases = form.phase_1_raw_capture.get('releases', {}).get('entries', []) %}
{% set latest_tag = releases[0].tag if releases else 'untagged' %}
{% set scanner_ver = form.get('_meta', {}).get('scanner_version', 'unknown') %}
*Generated by [stop-git-std](https://github.com/stefans71/stop-git-std) deep dive · {{ scan_date }} · scanned main @ `{{ head_sha }}` ({{ latest_tag }}) · scanner {{ scanner_ver }}*

*This report is an automated investigation, not a professional security audit. It may miss issues. If you are making a business decision, consult a security professional.*
